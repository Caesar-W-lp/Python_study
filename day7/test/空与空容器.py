# 作者:李一安
# 2025年02月10日18时19分20秒
# 1713450722@qq.com

# 空与空容器并不是一个东西
# 所有的空容器在布尔值上都被视为False，因此可以用if语句进行判断。
# None在布尔值上也被视为False，因此可以用if语句进行判断。
# python中一切皆对象，所有的容器，不论是否为空容器，它都是作为对象进行存储，
# 所以字符串作为一个容器，并不是以ASCII码的形式进行存储，故而空字符串并不是以0进行存储。（这是与C语言不同的）
# None也是一个空对象，但它表示的是此处什么都没有的意思

